For calibration:
	python Scanner.py
	Adjust parameter in ImageTaker.py 
		1	Offset  x/y for full view of the package
		2	Pin and Letter threshold for optimum detection (which mean the square should present on each pin (2 per pin))
	Note: restart Scanner after each modification to view the result

For production
	 python main.py
